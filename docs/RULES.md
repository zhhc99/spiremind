# RULES

- 在 `docs/` 下用 markdown 组织文档. 文档应该用中文书写, 使用英文标点.
- 代码应该自述功能, 非必要不编写注释.
- 禁止过度设计. 这包括但不限于: 让代码变长的架构拆分, 隐瞒错误的 catch 块.
- 禁止在代码中遗留旧逻辑.

## GPT / Codex

- 禁止使用 "不是...而是..." 表述.
- Failfast 原则.
- 仅有一处调用的代码不该成为单独函数, 除非它和上下文有明确区分的语义. 该原则是 "禁止过度设计" 的补充.
